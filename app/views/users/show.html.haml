= render :partial => 'shared/flash', :object => flash if flash.present?
#profile.grid_10.alpha.omega
  %div
    .namebar
      .name&= @user.name
      .actions= button_to 'Edit', edit_user_path(@user), :method => :get if current_user == @user
    %div= @user.email